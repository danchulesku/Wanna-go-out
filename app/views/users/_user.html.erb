<span class="user flex-nowrap">

  <span class="user-avatar">
    <%= small_avatar(user) %>
  </span>
  <span class="user-name">
    <% if request.path == user_path(user) %>
      <%= short_name(user.name) %>
    <% else %>
      <%= link_to short_name(user.name), user_path(user) %>
    <% end %>
  </span>

</span>
